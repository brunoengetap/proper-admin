Objetivo atual: implementar apenas a ETAPA 3 — aba Preventivas no detalhe da máquina.

Regras obrigatórias:
- não alterar machineKey()
- não alterar syncFromGS() nem syncToGS() — apenas usar
- não alterar _renderPartsEditor() nem saveMachineParts()
- não alterar a estrutura do db local (chave proper_admin_v2)
- não usar frameworks externos — HTML/CSS/JS puro
- não remover nenhuma aba existente (Resumo, Peças, Fotos)
- não alterar o comportamento das abas existentes
- não alterar campo_index.html
- não reescrever o arquivo do zero

O prompt completo com todas as especificações técnicas desta etapa
está no arquivo etapa3_admin_preventivas.txt anexado a esta tarefa.
Seguir aquele documento como fonte de verdade para implementação.
